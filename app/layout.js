export const metadata = { title:"writing-checker", description:"AI-powered tool that analyzes student writing for authenticity, voice consistency, and AI-generation signals." };
export default function RootLayout({ children }) {
  return (<html lang="en"><body>{children}</body></html>);
}
